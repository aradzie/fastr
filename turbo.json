{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "compile": {
      "dependsOn": ["^compile"],
      "outputs": ["lib/**"]
    },
    "test": {
      "dependsOn": ["compile"],
      "outputs": []
    },
    "clean": {
      "cache": false
    }
  }
}
